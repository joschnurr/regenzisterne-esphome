# Echte Zugangsdaten und Build-Artefakte nie ins Git.
secrets.yaml
.esphome/
*.bin

# Laufzeit-Zustand des ESPHome-Dashboards (vom Container als root angelegt).
.device-builder*

# Betriebs-/Sicherungsdateien des Dashboards, nie ins Repo.
*.log
*.bak*
